# Install and run the website in a local environment

- Please Clone the reporsitory with git clone command
- After clonong, install the packages with "npm run install"
- run the website with "npm run dev" 

